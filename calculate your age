let a = confirm(
  "Click on 'OK to calculate your age or you can exite using 'CANCEL'"
);
if (a == true) {
  let b = prompt("Enter your 'Birth year' to calculate your age =");
  let age = Number(b);
  const d = new Date();
  let year = d.getFullYear();
  let k;
  k = year - age;
  alert(`your age is ${k}.`);
} else {
  alert("You have cancled.");
}
